spring:
    config:
        import: file:./sidepeek_backend_secret/application-test.yml
    jpa:
        show-sql: true
        open-in-view: false
        hibernate:
            ddl-auto: create
    flyway:
        enabled: false
